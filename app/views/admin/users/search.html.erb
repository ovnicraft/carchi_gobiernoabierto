<h1>Buscar usuario entre los miembros de la web</h1>

<p>Introduce el email de la persona que quieres añadir.</p>

<%= form_tag 'search', :method =>:get do %>
  <%= hidden_field_tag :t, params[:t] %>
  <p>
    <label for="email">Email</label>
      <%= text_field_tag :email %>
  </p>
  <p><%= submit_tag 'Buscar' %></p>
<% end %>