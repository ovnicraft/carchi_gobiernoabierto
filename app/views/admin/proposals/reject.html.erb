<h1>Rechazando propuesta</h1>

<%= form_for @proposal, :url => do_reject_admin_proposal_path(@proposal), :html => {:method => :put} do |f| %>
  <%= error_messages(f.object) %>

  <%= hidden_field_tag :return_to, params[:return_to] %>
  <p>
    Tienes la opción de enviar al proponente un email diciéndole que su propuesta ha sido rechazada por
    no ajustarse a las Normas de uso
  </p>
  <p>
    <%= submit_tag "Rechazar y enviar email", :name => "reject_and_mail" %>
    <%= submit_tag "Sólo rechazar", :name => "reject" %>
  </p>
<% end %>