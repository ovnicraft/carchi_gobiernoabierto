<%= RiCal.Calendar do |cal|
  cal.add_x_property('X-WR-CALNAME',"#{Settings.site_name}::Eventos")
  @event.to_ics(cal) {{:url => url_for(:action => "show", :id => @event, :only_path => false)}}
end %>
